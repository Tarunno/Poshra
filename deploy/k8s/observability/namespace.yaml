apiVersion: v1
kind: Namespace
metadata:
  name: observability
  labels:
    # The poshra namespace's network policies name this label to let Loki and
    # Tempo reach MinIO.
    kubernetes.io/metadata.name: observability
    app.kubernetes.io/part-of: poshra
